language: ruby
sudo: false
bundler_args: --without development
script: "bundle exec rake $CHECK"
notifications:
  email: false
rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.6
  - 2.2.2
  - ruby-head

env:
  - "CHECK=spec"

matrix:
  allow_failures:
    - rvm: ruby-head
